/** Header logo 1000×180 px — bundled so production /static/ manifest hashing works. */
import headerLogoUrl from '@/assets/brand/vuvoz-header-logo.png'

export const BRAND_LOGO_WIDTH = 1000
export const BRAND_LOGO_HEIGHT = 180
export const BRAND_LOGO_ASPECT = BRAND_LOGO_WIDTH / BRAND_LOGO_HEIGHT

export const BRAND_LOGO_URL = headerLogoUrl

/** Square hero logo (homepage). */
import heroLogoUrl from '@/assets/brand/zeleniy-schet-hero-logo.png'
export const HERO_LOGO_URL = heroLogoUrl
