# Чеклист запуска «Зелёный счёт» (P0)

## Сервер

- [ ] `python manage.py migrate` (включая `0025_support_config`, `0026_request_pending_confirmation`)
- [ ] `python -m pytest tests/ -v` — все тесты зелёные
- [ ] `npm run build` в `frontend/`, `collectstatic`, перезапуск nginx/gunicorn
- [ ] `.env`: секреты, `ALLOWED_HOSTS`, SMTP (если нужны письма)

## Контент

- [ ] Логотипы: в git `frontend/src/assets/brand/` — после `build_frontend.sh` + `collectstatic` отображаются на всех страницах
- [ ] `media/` на сервере: фото материалов и товаров (скопировать с dev или загрузить в админке; папка не в git)
- [ ] В админке: ≥4 материала с фото, ценой и `sort_order`
- [ ] Проверка главной без логина: калькулятор (несколько видов сырья), каталог `/materials`
- [ ] Заявка на вывоз → вкладка «Заявки на вывоз»
- [ ] Регистрация учреждения → «Регистрация: учреждения»
- [ ] Регистрация компании → «Регистрация: компании»

## Юридическое

- [ ] Страница политики конфиденциальности (ссылка в формах)
- [ ] Чекбокс согласия на ПДн (уже в модалке регистрации)

## Мониторинг

- [ ] `GET /api/health/` отвечает 200
- [ ] Логи ошибок (Sentry или аналог) — по желанию P1

## После запуска (P1)

- Email админу при новой заявке
- Яндекс.Метрика, контактный телефон в шапке
- Реальные отзывы на главной
